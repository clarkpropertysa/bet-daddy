{
  "name": "bet-daddy",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev"
  },
  "devDependencies": {
    "prisma": "^6.1.0",
    "vercel": "^59.9.1"
  }
}
